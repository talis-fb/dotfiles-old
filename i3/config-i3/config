# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Roboto Bold 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet



#--------------#
#   Terminal
#--------------#
# Iniciar o Terminal
bindsym $mod+Return exec i3-sensible-terminal



#--------------#
#   dmenu
#--------------#
# start dmenu / Forma padrão
#bindsym $mod+d exec dmenu_run

#Start dmenu personalizado
#bindsym $mod+d exec "dmenu_run -nf '#F8F8F2' -nb '#282A36' -sb '#6272A4' -sf '#F8F8F2' -fn 'monospace-10' -p 'dmenu%'"

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# ROFI
bindsym $mod+d exec --no-startup-id "rofi -show drun -font \\"DejaVu 9\\" -run-shell-command '{terminal} -e \\" {cmd}; read -n 1 -s\\"'"


#---------------#
#   Wallpaper
#---------------#
exec --no-startup-id feh --bg-fill $HOME/Imagens/download.jpeg



#------------------------------#
#   Atalhos Rápidos para APPs
#------------------------------#
#Thurnar
bindsym $mod+t exec thunar

#Firefox
bindsym $mod+i exec firefox

#Brave Browser
#bindsym $mod+i exec brave

#VS Code
bindsym $mod+c exec code



#----------------------------#
#   Navegação em Workspace
#----------------------------#

#Volta ao ultimo workspace usado, semelhante ao Alt+Tab
bindsym $mod+z workspace back_and_forth
bindsym $mod+Tab workspace back_and_forth

# Definição de VARIAVEIS
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Troca a AREA DE TRABALHO (workspace)
bindsym $mod+Prior workspace prev
bindsym $mod+Next workspace next_on_output 
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# MOVE a janela focada ao Workspace
bindsym $mod+Shift+Prior move container to workspace next
bindsym $mod+Shift+Next move container to workspace prev
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

#Move a janela focada ao Workspace e troca o Workspace atual também
bindsym $mod+Ctrl+Prior move container to workspace next workspace next_on_output 
bindsym $mod+Ctrl+Next move container to workspace prev workspace prev
bindsym $mod+Ctrl+1 move container to workspace number $ws1 workspace number $ws1
bindsym $mod+Ctrl+2 move container to workspace number $ws2 workspace number $ws2
bindsym $mod+Ctrl+3 move container to workspace number $ws3 workspace number $ws3
bindsym $mod+Ctrl+4 move container to workspace number $ws4 workspace number $ws4
bindsym $mod+Ctrl+5 move container to workspace number $ws5 workspace number $ws5
bindsym $mod+Ctrl+6 move container to workspace number $ws6 workspace number $ws6
bindsym $mod+Ctrl+7 move container to workspace number $ws7 workspace number $ws7
bindsym $mod+Ctrl+8 move container to workspace number $ws8 workspace number $ws8
bindsym $mod+Ctrl+9 move container to workspace number $ws9 workspace number $ws9
bindsym $mod+Ctrl+0 move container to workspace number $ws0 workspace number $ws0


#--------------#
#   Janelas
#--------------#
title_align center
default_border normal

# ==> ENCERRAR janela
bindsym $mod+BackSpace kill
bindsym $mod+Shift+q kill

# ==> MUDAR O FOCO entre as janelas
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
# Outra forma que vem padrão no i3
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

# ==> MOVER JANELA FOCADA 
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
# Outra forma que vem padrão no i3
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# ==> TROCAR para orientação HORIZONTAL
bindsym $mod+h split h

# ==> TROCAR para orientação VERTICAL
bindsym $mod+v split v

# ==> Por em FULLSCREEN a janela focada
bindsym $mod+f fullscreen toggle

# ==> Trocar o layout da Janela, container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split



#------------------------#
#   Janelas flutuantes
#------------------------#
# ==> Trocar entre o mode flutuante ou fixo da janela focada (tiling / floating)
bindsym $mod+Shift+space floating toggle

# ==> Opção para arrastar uma janela flutuante q é segurando $Mod
floating_modifier $mod

# ==> Troca o foco entre a janela flutuante e a fixa, uma vez que a flutuante smp fica a frente
bindsym $mod+space focus mode_toggle


# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child



#------------------------------#
# Configurações Modo 'Resize'
#------------------------------#
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"



#-----------#
#   Cores
#-----------#

client.focused          #6272A4 #6272A4 #F8F8F2 #6272A4   #6272A4
client.focused_inactive #44475A #44475A #F8F8F2 #44475A   #44475A
client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #282A36
client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36



#--------------#
#   i3-Gaps
#--------------#
#Borda e o espaçamento
for_window [class=".*"] border pixel 2 #*
gaps inner 5

#Distancia entre as janelas
gaps outer 1 


#--------------#
#   Áudio
#--------------#
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status



#----------------------------------#
#   Reload, restart e saida do i3
#----------------------------------#
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Home exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
#Shutdown the Machine
bindsym $mod+End exec "i3-nagbar -t warning -m 'You pressed the PowerOff shortcut. Do you really want to shutdown i3?' -B 'Yes, power-off' 'shutdown -h now'"



#--------------#
#   Polybar
#--------------#
#exec_always --no-startup-id $HOME/.config/polybar/launch.sh
#exec_always --no-startup-id "killall -q polybar"

#bindsym $mod+p exec killall -q polybar
exec --no-startup-id "polybar -r mybar"

#Setar o layout de Teclado abnt bar
exec --no-startup-id "setxkbmap br"



#---------------------#
#   BAR padrão do I3
#---------------------#
# bar {
#         status_command i3status
# }


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
